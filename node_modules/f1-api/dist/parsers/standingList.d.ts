import Parser from "./parser";
export default class StandingListParser extends Parser {
    private static instance;
    static getInstance(): StandingListParser;
    parse(data: any, extraParser: Parser): any;
}
