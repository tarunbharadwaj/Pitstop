"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getConstructorStandings = void 0;
const axios_1 = __importDefault(require("axios"));
const cheerio_1 = __importDefault(require("cheerio"));
const endpoints_1 = require("../endpoints/endpoints");
const getConstructorStandings = (year = new Date().getFullYear()) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        let constructorStandings = [];
        const response = yield (0, axios_1.default)(`${endpoints_1.dynamicLinks.rootLink}/${year}/${endpoints_1.dynamicLinks.constructorStandings}`);
        const $ = cheerio_1.default.load(response.data);
        $("tr").each(function () {
            const position = parseInt($(this).find("td:nth-child(2)").text().trim());
            const team = $(this).find("td:nth-child(3)").text().trim();
            const points = parseInt($(this).find("td:nth-child(4)").text().trim());
            if (!Number.isNaN(position) && team.length !== 0 && !Number.isNaN(points)) {
                const constructorStanding = {
                    position,
                    team,
                    points,
                };
                constructorStandings.push(constructorStanding);
            }
        });
        if (constructorStandings.length === 0) {
            throw new Error(" No data found");
        }
        return constructorStandings;
    }
    catch (error) {
        throw new Error(error);
    }
});
exports.getConstructorStandings = getConstructorStandings;
//# sourceMappingURL=constructors-standings.js.map