"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFastestLaps = void 0;
const axios_1 = __importDefault(require("axios"));
const cheerio_1 = __importDefault(require("cheerio"));
const endpoints_1 = require("../endpoints/endpoints");
const getFastestLaps = (year = new Date().getFullYear()) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        let fastestLaps = [];
        const response = yield (0, axios_1.default)(`${endpoints_1.dynamicLinks.rootLink}/${year}/${endpoints_1.dynamicLinks.fastestLap}`);
        const $ = cheerio_1.default.load(response.data);
        $("tr").each(function () {
            const grandPrix = $(this).find("td.width30.dark").text().trim();
            const driverFirstName = $(this).find("td.width25.dark.bold > span.hide-for-mobile").text().trim();
            const driverLastName = $(this).find("td.width25.dark.bold > span.hide-for-tablet").text().trim();
            const car = $(this).find("td.width25.semi-bold.uppercase").text().trim();
            const time = $(this).find("td:nth-child(5)").text().trim();
            const driver = driverFirstName.concat(" ", driverLastName);
            console.log(driver, grandPrix, car, time);
            if (driver.length !== 0 && grandPrix.length !== 0 && car.length !== 0 && time.length !== 0) {
                const fastestLap = {
                    driver,
                    grandPrix,
                    car,
                    time,
                };
                fastestLaps.push(fastestLap);
            }
        });
        if (fastestLaps.length === 0) {
            throw new Error("No data found");
        }
        return fastestLaps;
    }
    catch (error) {
        throw new Error(error);
    }
});
exports.getFastestLaps = getFastestLaps;
//# sourceMappingURL=fastest-laps.js.map